(executable
 (public_name ocaml_htmx)
 (name main)
 (libraries dream-html base tyxml ocaml_htmx dream)
 (preprocess
  (pps lwt_ppx)))

;;;;(rule
; (deps main.eml.ml)
; (targets main.ml)
;(action (run dream_eml %{deps} --workspace %{workspace_root})))
